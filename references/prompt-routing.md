<!-- Emitted verbatim by scripts/prompt-router.sh as UserPromptSubmit context on every free-text prompt in a ck-code project. Every line costs tokens on every prompt: keep it a routing table, never a workflow. Lines starting with "<!--" are stripped. The intent table mirrors skills/guide Mode B — keep both in sync. -->
ck-code router — this project uses ck-code. Before acting on this prompt, pick the best-fit ck-code skill and invoke it with the Skill tool: announce it in one line (`→ /ck-code:<skill> <args>`), then call it. The prompt itself is the consent — never ask "should I use /ck-code:fix?" first.
Routing (first match wins): bug, crash or regression in built code → `fix` · implement or continue an existing story → `build` · new functionality with no story → `plan --quick <brief>` (small) or `plan` (large) · stakeholder-facing feature spec → `spec` · architecture or tech choices → `design` · expert/guide skills or house conventions → `team` · progress or next story → `track` · commit, PR or "ship it" → `ship` · explain or verify what was built → `explain` · indexes/stories/plan look broken → `doctor` · issue tracking or board settings → `config` · GitHub drift (merged PR not reflected) → `sync` · unsure → `guide`.
Do not route when: a ck-code skill is already running and this prompt answers its question — continue it; the prompt is a question, a read-only or non-code request — answer directly; the prompt is a one-off edit no story covers — do it, but first read the matching expert or guide skill under `.claude/skills/` and follow its conventions, test first.
Never route to a skill whose prerequisite is missing — name the prerequisite instead (`build` needs a story in `tasks/`; `plan` needs `docs/architecture/`).
